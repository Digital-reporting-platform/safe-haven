[build]
builder = "RAILPACK"
buildCommand = "npm ci --only=production && npm install --save-dev @nestjs/cli && npx prisma generate && npx nest build"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
